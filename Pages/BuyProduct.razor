@page "/product"

<PageTitle>Produto</PageTitle>

<div class="bg-primary text-white h-[3rem] flex justify-center items-center md:hidden ">
    HOME &gt; CATEGORIAS &gt; MEMÓRIAS RAM
</div>
<div class="flex flex-col w-full p-[1rem]  h-screen justify-center items-center z-1 gap-[1rem]">
    <div class="w-full md:w-1/2 border flex justify-center bg-gradient-to-br from-green-600 to-blue-800">
        <img class="max-w-56 bg-gradient-to-br from-green-600 to-blue-800"  src="icon-192.png" alt=""/>
    </div>
    <div class="flex flex-col items-center justify-center md:max-w-1/2 max-h-1/2 p-[2rem] border text-[0.7rem] md:text-[1rem] text-white flex flex-col gap-[1rem] border rounded-md shadow-md bg-gradient-to-br from-blue-400 to-green-600 
            hover:shadow-2xl p-4">
        <h1>Código Limpo</h1>
        <p>Mucho texto mucho texto mucho texto mucho texto mucho texto mucho texto mucho texto mucho texto mucho texto mucho texto mucho texto mucho texto mucho texto mucho texto mucho texto mucho texto mucho texto mucho texto mucho texto mucho texto mucho texto mucho texto</p>
        <div class="flex flex-col">
            <p class="text-xl font-bold border-0 rounded-2xl bg-gradient-to-r from-green to-blue-500 text-blue-950">R$ 78,99</p>
            <p>À vista com 15% de desconto no boleto ou pix</p>
        </div>
        <div class="flex flex-row gap-[0.5rem]">
            <p class="text-[1rem] material-icons">credit_card</p>
            <p>ou até 3x no cartão</p>
        </div>
        <div class="flex items-center justify-between pt-2 border-t"></div>
        <div class="flex gap-2 items-center justify-center">
             <button class="flex items-center px-3 py-1 text-sm font-medium text-white rounded-md bg-secondary hover:bg-secondary/80 cursor-pointer">
                <span class="mr-1 text-sm material-icons">add_shopping_cart</span> Adicionar ao carrinho
            </button>
            <button class="flex items-center px-3 py-1 text-sm font-medium text-white rounded-md bg-primary hover:bg-primary/80 cursor-pointer">
               <span class="mr-1 text-sm material-icons">shopping_cart</span> Comprar agora
            </button>
        </div>
    </div>
</div>
